# 반도체 7대 실데이터 신호 — 실측 정합성 수정 체크리스트

목표. 섹터분석 > 반도체 > "7대 실데이터 신호 & 5국면 진단" 화면의 헤더 수치·배지·기간
버튼·차트 단위를 전부 실제 시계열에서 계산한 값으로 맞춘다.

## 백엔드 (`backend/core/semi_cycle_engine.py`)

- [x] 주가 시계열 슬라이스 상수를 주간 기준으로 교정 (`[-1250:]` → `[-260:]`)
- [x] `data_points_count`를 실제 포인트 수/주기로 산출 (하드코딩 "2500거래일" 제거)
- [x] 대장주 낙폭 / 업종 지수 차트 시계열을 낙폭(%) 시계열로 변환 (헤더 단위와 일치)
- [x] 52주 고점·현재가·낙폭을 yfinance 실데이터에서 계산
- [x] 월간 5개 지표의 현재값을 시계열 마지막 값에서 산출
- [x] YoY 배지를 12개월 전 대비 실측 증감률로 계산
- [x] 실질 수출물량 헤더값을 차트와 같은 산식(물량지수)으로 통일
- [x] 가동률·재고 `3M` 이동평균을 실측으로 계산
- [x] status(호황/중립/둔화)를 임계선 기반으로 판정
- [x] `signals_count`를 판정 결과에서 집계 (반도체 한정)
- [x] description 내 하드코딩 수치를 실측값 보간으로 교체
- [x] 미사용이 된 `monthly_dates_10y` 생성 블록 정리

## 프런트엔드 (`dashboard/src/components/SemiFundamentalSignals.tsx`)

- [x] 기간 슬라이스를 `길이 > 300` 휴리스틱 대신 날짜 포맷(월간/주간)으로 분기
- [x] 주간 시계열 슬라이스 상수 52/156/260/520 적용
- [x] 데이터 개수 라벨 "거래일" → "주"
- [x] 정적 Fallback의 주가 시계열도 낙폭(%)으로 변환

## 검증

- [x] 로컬 백엔드 기동 후 `/api/v1/analyze/semi-cycle/macro-signals` 응답 실측 확인
- [x] 브라우저에서 1년/3년/5년/10년 버튼별 x축 구간이 실제로 달라지는지 확인
- [x] 7개 지표 헤더값 = 각 차트 마지막 값 일치 확인
- [x] `pytest` 통과 확인
- [x] 시맨틱 커밋

---

# 2차 — 국면 판정 실데이터화 + 업종별 실데이터 분리

## 백엔드

- [x] 업종별 데이터 소스 정의(`INDUSTRY_PROFILES`) — 대장주 티커, 업종 지수(공식/바스켓)
- [x] yfinance 배치 주간 종가 로더 (`yf.download` 다종목 1회 호출)
- [x] 대표주 동일가중 바스켓 지수 산출 (공식 지수가 없는 업종용)
- [x] 지표별 연속 점수화(`_score`) — 임계선 기준 [-1, +1] 정규화
- [x] 가중 종합 점수 → 5국면 매핑(`_diagnose_phase`)
- [x] 13개월 소급 판정 타임라인을 실제 과거 데이터로 재계산
- [x] `state_transition`(직전 국면·전환 시점·방향)을 타임라인에서 유도
- [x] `current_state`·`weighted_score`·`score_gauge_pct`·`summary_comment`·`current_action` 하드코딩 제거
- [x] 공표통계 미연동 업종은 해당 지표를 `available: false`로 명시 (반도체 수치 재사용 금지)
- [x] `get_industries_summary`를 동일 엔진으로 실측 산출 (state/trend/is_partial)
- [x] `industry_meta_map` 서술형 상수 제거
- [x] 실제 주가/지수 시계열과 52주 고점 대비 낙폭 시계열 동시 제공 (`has_drawdown_toggle`)

## 프런트엔드

- [x] `available: false` 지표를 비활성 행으로 렌더 (차트/아코디언 없음)
- [x] 국면 색상을 백엔드 `phase_color` 기준으로 렌더 (emerald 하드코딩 제거)
- [x] "실데이터 7개 신호" 문구를 실제 지표 수로 대체
- [x] 중복되던 '사이클 시계 (2D Quadrant)' 서브메뉴 탭 제거
- [x] 7대 실데이터 상단 업종 선택 칩 바 제거 및 단일 업종 집중 뷰로 최적화
- [x] 대장주/업종지수 차트 기본 뷰를 실제 주가/지수($/pt) 추세선으로 변경
- [x] `[실제 주가/지수 | 고점대비 낙폭(%)]` 인터랙티브 뷰 전환 토글 스위치 구현

## 검증

- [x] 업종 11개 전부 API 200 및 지표 구성 확인
- [x] 반도체 외 업종에서 반도체 수치가 노출되지 않는지 확인
- [x] 타임라인 국면이 실제 점수 시계열과 일치하는지 확인
- [x] `pytest` 및 Next.js 프로덕션 빌드 통과
- [x] 브라우저 확인 및 커밋 완료
