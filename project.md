# Project: ETF One

## 1. 프로젝트 비전

* **목표**: 파편화된 ETF 데이터를 통합하여 실물 경제 지표와 연계된 심층 비교 분석 서비스 제공.
* **차별점**: 기존 서비스(ETFcheck 등)의 한계를 넘어 **실질 보수(TER+매매비용)**, **종목 간 상관관계**, **포트폴리오 중복도**를 실시간/배치 하이브리드 방식으로 분석.
* **핵심 원칙**:
1. **Data Stability**: 장중 1시간 주기 배치 수집 및 18시 일마감 데이터 확정.
2. **Real-time Precision**: 종목 비교 시에만 해당 종목의 실시간 데이터를 핀포인트로 수집.

---

## 2. 데이터 수집 및 관리 전략 (Data Strategy)

| 구분 | 주안점 | 수집 주기 | 비고 |
| --- | --- | --- | --- |
| **Batch Update** | 전체 종목 시장 지표 및 시세 | 장중 1시간 단위 (09:00~15:30) | 서버 부하 최소화 및 기본 데이터 캐싱 |
| **Daily Closing** | 확정 NAV, 괴리율, 일일 수익률 | 매일 18:00 (일마감) | 분석 리포트 생성을 위한 Golden Source |
| **On-Demand** | 비교 대상 종목의 최신 호가 및 지표 | 비교 요청 시 즉시(Real-time) | 분석 에이전트 호출과 동시 실행 |

---

## 3. Multi-Agent 설계 명세

### 3.1 [Agent 1] 데이터 수집/정제 (The Harvester)

* **Role**: 금융 포털 및 운용사 공시 자료 수집 엔진.
* **Instruction**:
* 스케줄러에 따라 1시간 단위로 포털 크롤링을 수행하여 DB를 갱신한다.
* 18시 정각에 전체 종목의 마감 데이터를 수집하여 일일 확정 레코드를 생성한다.
* Orchestrator의 요청 시 특정 종목 코드에 대한 실시간 Scraping을 즉시 수행한다.


* **Tools**: Playwright (Dynamic Crawling), PDF Parser (Prospectus analysis), API Connector.

### 3.2 [Agent 2] 데이터 가공/분석 (The Quant)

* **Role**: 금융 공학 기반 지표 산출 및 리스크 분석.
* **Instruction**:
* 수집된 로우 데이터를 바탕으로 실질 TER, 샤프 지수, MDD를 계산한다.
* 비교 대상 간의 종목 중복도(Overlap) 및 상관계수(Correlation)를 산출한다.
* 18시 마감 데이터를 분석하여 '오늘의 테마 유입 TOP 5' 등 인사이트를 도출한다.


* **Tools**: Pandas, NumPy, Statsmodels.

### 3.3 [Agent 3] 데이터 Orchestration (The Conductor)

* **Role**: 전체 워크플로우 제어 및 데이터 서빙 최적화.
* **Instruction**:
* 일반 조회 요청 시 캐시된(1시간 주기) 데이터를 즉시 반환한다.
* 비교 분석 요청 시 Agent 1에게 실시간 수집을, Agent 2에게 비교 분석을 명령하는 파이프라인을 가동한다.
* 사용자 의도에 맞는 시각화 포맷(차트, 테이블)을 결정하여 API로 전달한다.



### 3.4 [Agent 4] 서비스 개발 (The Architect)

* **Role**: 웹 인터페이스 및 API 시스템 구축.
* **Instruction**:
* Next.js와 Tailwind CSS를 사용하여 반응형 대시보드 및 비교 페이지를 개발한다.
* Orchestrator와 통신하는 FastAPI 기반의 백엔드를 구축한다.
* 데이터 업데이트 시각을 명확히 표시하고 인터랙티브 차트(D3.js)를 구현한다.



### 3.5 [Agent 5] 서비스 테스트 (The Guardian)

* **Role**: 데이터 정합성 검증 및 품질 보증(QA).
* **Instruction**:
* 18시 마감 데이터가 실제 거래소 데이터와 일치하는지 샘플링 검사를 수행한다.
* 실시간 수집 시 차단(Anti-bot) 대응 능력을 테스트한다.
* 종목 비교 로직의 산술적 오류 여부를 최종 확인한다.

---

## 4. 개발 로드맵 (Milestones)

1. **Stage 1: 하이브리드 수집 인프라**
* 1시간 배치 및 18시 일마감 스케줄러 구현.
* 금융포털 크롤링 모듈 안정화.


2. **Stage 2: 핀포인트 분석 엔진**
* 종목 비교 시 즉시 수집(On-demand) 프로세스 연동.
* 종목 간 상관관계 및 중복 비중 계산 로직 개발.


3. **Stage 3: 사용자 대시보드**
* ETF 비교 대시보드 UI/UX 구현.
* 실시간/배치 데이터 상태 표시등 구현.


4. **Stage 4: 최종 검증 및 배포**
* 금융 데이터 오차율 0.1% 미만 검증.
* Antigravity 환경 내 최종 배포.

---

## 5. Deployment & Testing Rules (개발 및 배포 원칙)

1. **로컬 우선 검증 (Local Verification First)**: 기능 개발 및 수정 후 무조건 로컬 서버(`http://localhost:3000`, `http://localhost:8000`)에서 정상 동작 및 반환 데이터 무결성 측정(빈 값, NA 등 에러 체크)을 먼저 진행한다.
2. **사용자 승인 후 배포 (User Approval for Deployment)**: 로컬 검증이 끝난 후, 사용자에게 로컬 테스트 결과를 보고하고 **사용자의 "오케이(승인)" 사인이 떨어져야만** `git push`를 통한 Render/Vercel 상용 서버 배포를 진행한다.
3. **명령어 자동 실행 (Auto-Run Commands)**: 폴더 이동, 패키지 설치, 스크립트 실행 등 개발에 필요한 기본 터미널 명령어는 사용자 개입을 최소화하기 위해 자동 실행 권한(`SafeToAutoRun: true`)을 적극 부여한다. 단, 시스템 보안 상 에디터단에서 강제 확인을 요구하는 레포지토리 푸시(`git push`) 등은 예외로 둔다.
## 추가 개발

0. 일부 오류 수정 : 주가/상대수익율 차트에서 미국 etf의 경우 비교 지수를 해당 종목과 관련된 미국 지수로 해야 함
1. ETF 핵심 지표 확장:
지금의 기본 데이터(현재가, 등락률) 외에 백엔드 스크래퍼를 고도화하여 배당 수익률(분배율), 총 보수(수수료), 과거 1/3/6개월 수익률 추이 등의 알짜 정보들을 긁어와 화면에 추가로 표시해 주는 기능.
2. 시각화(차트) 고도화:
현재 있는 차트들을 조금 더 정밀하게 다듬거나, 여러 ETF의 수익률 곡선을 겹쳐서 비교해 볼 수 있는 '기간별 누적 수익률 비교 차트' 추가.
3. 개인화(저장 기능) 추가:
사용자가 즐겨찾기한 종목들이 브라우저를 껐다 켜도 남아있도록 로컬 스토리지에 저장시켜 주거나, 나만의 '관심 종목 그룹(포트폴리오)'을 만들어 저장할 수 있는 기능.
4. 데이터 소스 다변화 (예: KIS 증권사 API 연동):
기존에 테스트용으로 만들어두신(폴더 내에 test_kis 파일들이 있더군요!) 증권사 API를本格적으로 파이썬 백엔드에 연동시켜, 실시간 호가 등 더 빠르고 정확한 증권사 공식 데이터를 가져오는 작업.

